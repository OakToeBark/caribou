<% if @reviews.blank? %>
  <span class="center">There is no review yet</span>
<% end %>

<% @reviews.order("id desc").each do |r| %>
  <hr>
  <div class="row">

    <div class="col m1">
      <!-- Bugged with facebook authentication but not google & also text portion of review not showing -->


      <!--<% if(!user_signed_in?) %>
        <li><%= link_to "Sign Up", new_user_registration_path %></li>
        <li><%= link_to "Login", new_user_session_path %></li>
      <% else %>-->
          <%= image_tag avatar_url(r.user), class: "circle responsive-img avatar-small" %>
      <!--<% end %>-->

    </div>

    <div class="col m10">
      <div>
        <strong><%= r.user.name%></strong>
        <!-- current_user review model fucked up-->
        <span class="right"><%= r.created_at %></span>
      </div>
      <!-- <div id="stars_<%= r.id %>"></div> -->
      <div><%= r.star %></div>
      <div><% r.comment %></div>
      <div>
        <%= link_to "Remove", [@project, r], method: :delete, data: {confirm: "Are you sure?"} %>
      </div>

    </div>
  </div>

<!-- <script>
  $('#stars_<%= r.id %>').raty({
    path: '/assets',
    readOnly: true,
    score: <%= r.star %>
  });
</script> -->

<% end %>
